[package]
name = "adrian-kernel"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]

[features]
default = []
# Enables hosted-mode compilation: links std, and swaps privileged
# bare-metal primitives (raw x86 port I/O, the no_std panic handler)
# for host-safe equivalents. Bare-metal builds leave this off.
std = []
